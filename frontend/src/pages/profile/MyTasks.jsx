const MyTasks = (props) =>{
    return(
        <>
        <p>A clean list of every task assigned to you from every project in the database. (inside the profile)</p>
        </>
    )
}
export default MyTasks;